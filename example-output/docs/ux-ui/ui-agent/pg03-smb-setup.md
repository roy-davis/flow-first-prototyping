> Provisional: generated by proto.oneshot/full artifact rerun from prd.md and existing docs/ux-ui artifacts. Review before treating as authoritative.

# PG03 SMB Setup

PRODUCT AND DESIGN CONTEXT

Native Android TV audiobook player for SMB and first-class cloud sources. Use a quiet, dark, TV-scale interface with explicit D-pad focus, overscan-safe spacing, cached-library browsing, and source-specific recovery. Do not render instruction fields as visible UI text; literal copy comes only from CONTENT or COPY REQUIREMENTS.

---

SCREEN CONTEXT

SMB Setup sits at Sources Select SMB, Source Repair Edit SMB credentials.. It exits to Folder Picker, Source Repair, Sources.. This screen matters because collect and validate smb source details before folder selection.

---

SCREEN SPECIFICATION - PG03 SMB Setup

Screen ID: PG03 | TV: Critical | Scope: In scope

Primary purpose: Collect and validate SMB source details before folder selection.

Primary job: Connect to a local Samba/Windows share securely.

User and usage context:
- Primary actor: Sam the Server Steward
- State of mind: Focused, remote-driven, intolerant of unclear loading or source failure.
- Physical/device context: Android TV, D-pad remote, couch distance, home network/cloud variability, overscan constraints.

Content priority:
- PRIMARY: Primary: host/share fields and Test connection
- SECONDARY:  Secondary: credential mode and error detail
- TERTIARY: Low-priority metadata

ANTI-GOALS:
- Do not add screens, tabs, bottom nav, hover-only controls, phone gestures, lorem ipsum, or raw provider/credential data.

STATES TO RENDER - produce all listed states as distinct frames:

STATE 1 - CREDENTIALS FORM
Fields ready for host/share/credentials. Transition behavior: Test connection -> testing_connection.

STATE 2 - TESTING CONNECTION
Validation in progress. Transition behavior: Success -> Folder Picker; failure -> connection_error.

STATE 3 - CONNECTION ERROR
Host/share/auth validation failed. Transition behavior: Retry or edit fields.

STATE 4 - ANONYMOUS LOGIN
Anonymous mode selected. Transition behavior: Username/password hidden.

ACTIONS:
- Test connection -> Validate SMB source; precondition: Required fields present or anonymous mode selected
- Use anonymous login -> Toggle credential mode; precondition: Always
- Continue -> Go to folder picker after validation; precondition: Connection valid
- Cancel -> Return to Sources or Source Repair; precondition: Always

IN-FLOW CONTEXTUAL ACTIONS:
- Connection error panel -> Focused field action -> Move focus to host/share/credential group -> Preserve entered values

TRANSITION CONTRACT:
- Entry/from Sources via Select SMB -> SMB Setup (credentials_form). Back/exit: Back returns Sources. Blocked/error: N/A.
- Entry/from SMB Setup via Test connection -> SMB Setup (testing_connection). Back/exit: Cancel returns credentials_form. Blocked/error: Validation error -> connection_error.
- Entry/from SMB Setup via Validation success -> Folder Picker (folder_browsing). Back/exit: Back returns SMB Setup with source context. Blocked/error: N/A.
- Entry/from Source Repair via Edit SMB credentials -> SMB Setup (credentials_form repair mode). Back/exit: Back returns Source Repair. Blocked/error: Bad credentials stays on form.

DATA:
- Host/share: User input, N/A. May be long
- Credential mode: User input, N/A. Username/password or anonymous
- Validation result: Provider API, Real-time. Specific error category

DATA RANGE AND EDGE CASES:
- Empty / zero data: Disable Test connection until required fields exist
- Typical data: Host, share, username, password
- Maximum / dense data: Long host/share paths wrap without hiding action
- Long text / overflow: Fields scroll horizontally or wrap label/value safely
- Text expansion / localization: Form groups stack vertically
- Loading / slow response: Testing state keeps focus on action and blocks duplicate test
- Offline / degraded connection: Host unreachable message explains local network dependency
- Permission / restricted access: Bad credentials and permission denied have separate messages
- Duplicate or concurrent action: Disable Test while testing
- Responsive / safe area: Form stays inside safe area
- Keyboard / focus: Field order follows form reading order

INTERACTION STATES:
- Test connection: default visible; focus scale/glow; pressed state; disabled by precondition; loading blocks duplicate action; success updates state; error shows recovery.
- Use anonymous login: default visible; focus scale/glow; pressed state; disabled by precondition; loading blocks duplicate action; success updates state; error shows recovery.
- Continue: default visible; focus scale/glow; pressed state; disabled by precondition; loading blocks duplicate action; success updates state; error shows recovery.
- Cancel: default visible; focus scale/glow; pressed state; disabled by precondition; loading blocks duplicate action; success updates state; error shows recovery.

RESPONSIVE AND HARDENING NOTES:
- Use TV-safe margins of 48dp minimum, 64dp preferred.
- Focus must be visible by scale plus border/glow, not color alone.
- Long text wraps or truncates without overlapping primary actions.
- Offline/degraded/source failure states preserve cached context where possible.

VISUAL INDICATORS:
- Inline field error: Specific invalid field
- Testing status: Validation in progress
- Secure field mask: Password hidden

LAYOUT REGIONS:
- Header: SMB setup context; required content: Title, source type, Back
- Form: Source details; required content: Host, share, credential mode, credentials
- Status/action: Validation; required content: Test, errors, Continue

CONTENT:
- Page title: Connect SMB source
- Primary action label: Test connection
- Error heading: Connection failed
- Error body: Check the host, share, and credentials, then try again.
- Success message: Connection verified.
- Field labels / hints: Host; Share; Username; Password; Anonymous login

COPY REQUIREMENTS:
- Button labels: Test connection; Use anonymous login; Continue; Cancel
- Error messages: Connection failed - Check the host, share, and credentials, then try again.
- Empty state: None specified / None specified
- Confirmation / destructive copy: None specified
- Offline / degraded copy: None specified

MOCK DATA:
- Current book: Project Hail Mary by Andy Weir, Chapter 18 - Petrova Line, 9:42:18 of 16:10:44, 1.25x.
- Sources: Family NAS (SMB, healthy), Cloud Audiobooks (Google Drive, auth expired), Shared Classics (Dropbox, partial sync).
- Books: Project Hail Mary, The Left Hand of Darkness, The Hobbit, Dune, The Long Way to a Small Angry Planet.

FIDELITY:
Mid-fi - target tool Other. Generate realistic states, spacing, and copy suitable for Android TV visual review.

ACCEPTANCE CRITERIA:
- Sam can identify required SMB fields and test the connection with D-pad input.
- Testing state blocks duplicate validation and keeps feedback visible.
- Host/network, share, and credential failures are visually distinct.
- Anonymous mode hides irrelevant credential fields.
- Long host/share paths do not overlap primary actions.
